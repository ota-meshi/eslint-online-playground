import fs from "fs";
import { ESLint } from "eslint";

fs.writeFileSync("foo.js", "require('foo')");

const linter = new ESLint();

const result = await linter.lintFiles(["foo.js"]);
console.log(result);

process.exit();
